{
  "name": "windly",
  "displayName": "Windly - Tailwind CSS Organizer",
  "description": "Windly - group Tailwind class lists by intent and format them into tidy multiline blocks.",
  "icon": "images/windly.png",
  "version": "1.1.0",
  "publisher": "trishan9",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/trishan9/Windly.git"
  },
  "engines": {
    "vscode": "^1.80.0"
  },
  "categories": [
    "Formatters"
  ],
  "keywords": [
    "tailwind",
    "tailwindcss",
    "class sorting",
    "class grouping",
    "clean code",
    "pretty",
    "formatter",
    "tailwind organizer",
    "css utilities",
    "clsx"
  ],
  "main": "./dist/extension.js",
  "activationEvents": [
    "onCommand:windly.groupClasses"
  ],
  "contributes": {
    "commands": [
      {
        "command": "windly.groupClasses",
        "title": "Windly: Group Tailwind Classes"
      }
    ],
    "configuration": {
      "title": "Windly",
      "properties": {
        "windly.preferClsxWrapper": {
          "type": "boolean",
          "default": true,
          "description": "Wrap JSX className attributes in a multi-line clsx() call. Disable if you prefer multi-line strings instead."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "pnpm run package",
    "compile": "pnpm run check-types && pnpm run lint && node esbuild.js",
    "watch": "npm-run-all -p watch:*",
    "watch:esbuild": "node esbuild.js --watch",
    "watch:tsc": "tsc --noEmit --watch --project tsconfig.json",
    "package": "pnpm run check-types && pnpm run lint && node esbuild.js --production",
    "compile-tests": "tsc -p . --outDir out",
    "watch-tests": "tsc -p . -w --outDir out",
    "pretest": "pnpm run compile-tests && pnpm run compile && pnpm run lint",
    "check-types": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vscode-test",
    "release": "semantic-release"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "22.x",
    "@types/react": "^19.2.7",
    "@types/vscode": "^1.80.0",
    "@vscode/test-cli": "^0.0.12",
    "@vscode/test-electron": "^2.5.2",
    "clsx": "^2.1.1",
    "esbuild": "^0.27.1",
    "eslint": "^9.39.1",
    "@semantic-release/changelog": "^6.0.3",
    "@semantic-release/commit-analyzer": "^11.0.0",
    "@semantic-release/exec": "^6.0.3",
    "@semantic-release/git": "^10.0.1",
    "@semantic-release/github": "^11.0.0",
    "@semantic-release/release-notes-generator": "^12.0.0",
    "npm-run-all": "^4.1.5",
    "react": "^19.2.1",
    "semantic-release": "^23.0.0",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.48.1"
  }
}
